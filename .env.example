# Vault (local dev only — not a real secret)
VAULT_ADDR=http://localhost:8200
VAULT_DEV_ROOT_TOKEN=dev-root-token

# Ports (local dev only)
API_PORT=8000
MODEL_SERVER_PORT=8001
CHATBOT_PORT=8501
DB_PORT=5432
REDIS_PORT=6379
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
VAULT_PORT=8200

# Non-secret local dev flags
LOG_LEVEL=INFO
ENVIRONMENT=local

# Local dev service bootstrap (dev-only, non-secret)
DB_PASSWORD=localdev
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
